<Window x:Class="KamatekCrm.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:KamatekCrm.ViewModels"
             Height="500" Width="700">
    


    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Sol: Müşteri Listesi -->
        <DataGrid Grid.Column="0"
                  ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False"
                  Margin="0,0,10,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Ad" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Soyad" Binding="{Binding Surname}" Width="*"/>
                <DataGridTextColumn Header="E-posta" Binding="{Binding Email}" Width="*"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Sağ: Form -->
        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <TextBlock Text="Ad" Margin="0,5,0,0"/>
            <TextBox Text="{Binding NewOrEditCustomer.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <TextBlock Text="Soyad" Margin="0,5,0,0"/>
            <TextBox Text="{Binding NewOrEditCustomer.Surname, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <TextBlock Text="E-posta" Margin="0,5,0,0"/>
            <TextBox Text="{Binding NewOrEditCustomer.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <TextBlock Text="Telefon" Margin="0,5,0,0"/>
            <TextBox Text="{Binding NewOrEditCustomer.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Ekle" Command="{Binding AddCommand}" Width="80" Margin="0,0,5,0"/>
                <Button Content="Güncelle" Command="{Binding UpdateCommand}" Width="80" Margin="0,0,5,0"/>
                <Button Content="Sil" Command="{Binding DeleteCommand}" Width="80" Margin="0,0,5,0"/>
                <Button Content="İptal" Command="{Binding CancelCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>