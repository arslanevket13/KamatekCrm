<Window x:Class="KamatekCrm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:KamatekCrm.ViewModels"
        xmlns:views="clr-namespace:KamatekCrm.Views"
        Title="Kamatek CRM" Height="800" Width="1200">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

      
        <!-- Menü -->
        <Border Grid.Row="0" Background="LightGray" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Müşteriler" Command="{Binding ShowCustomersViewCommand}" Margin="5" Padding="10"/>
                <Button Content="İş Kayıtları" Command="{Binding ShowTicketsViewCommand}" Margin="5" Padding="10"/>
            </StackPanel>
        </Border>

        <!-- İçerik -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:CustomersViewModel}">
                    <views:CustomersView />
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:TicketsViewModel}">
                    <views:TicketsView />
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>