<UserControl x:Class="KamatekCrm.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KamatekCrm.Views"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
             xmlns:viewmodels="clr-namespace:KamatekCrm.ViewModels"
             xmlns:helpers="clr-namespace:KamatekCrm.Helpers"
             d:DataContext="{d:DesignInstance Type=viewmodels:CustomersViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="{StaticResource BackgroundColor}">

    <UserControl.Resources>
        <helpers:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Müşteri Yönetimi" 
                   FontSize="24" 
                   FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryColor}"
                   Margin="0,0,0,10"/>

        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Margin="0,0,0,10" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Müşteri Bilgileri" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryColor}"/>

                <!-- Customer Type Selection (RadioButtons) -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
                    <StackPanel.Resources>
                        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
                    </StackPanel.Resources>
                    <TextBlock Text="Müşteri Tipi:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,20,0"/>
                    <RadioButton Content="Bireysel" 
                                 IsChecked="{Binding NewCustomerType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Individual, Mode=TwoWay}"
                                 GroupName="NewCustomerType" 
                                 Margin="0,0,20,0" 
                                 VerticalAlignment="Center"/>
                    <RadioButton Content="Kurumsal" 
                                 IsChecked="{Binding NewCustomerType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Corporate, Mode=TwoWay}"
                                 GroupName="NewCustomerType" 
                                 VerticalAlignment="Center"/>
                </StackPanel>

                <!-- INDIVIDUAL CUSTOMER FIELDS (Visible only when Type = Individual) -->
                <Border Grid.Row="2" 
                        Visibility="{Binding NewCustomerType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Individual}"
                        Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Bireysel Müşteri Bilgileri" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <Label Content="TC Kimlik No" Style="{StaticResource ModernLabel}"/>
                                <TextBox Text="{Binding NewTcKimlikNo, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource ModernTextBox}" 
                                         MaxLength="11"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- CORPORATE CUSTOMER FIELDS (Visible only when Type = Corporate) -->
                <Border Grid.Row="2" 
                        Visibility="{Binding NewCustomerType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Corporate}"
                        Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Kurumsal Müşteri Bilgileri" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <Label Content="Şirket Ünvanı" Style="{StaticResource ModernLabel}"/>
                                <TextBox Text="{Binding NewCompanyName, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                <Label Content="Vergi Numarası" Style="{StaticResource ModernLabel}"/>
                                <TextBox Text="{Binding NewTaxNumber, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <Label Content="Vergi Dairesi" Style="{StaticResource ModernLabel}"/>
                                <TextBox Text="{Binding NewTaxOffice, UpdateSourceTrigger=PropertyChanged}" 
                                         Style="{StaticResource ModernTextBox}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,10,10">
                        <Label Content="Ad Soyad *" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,10,10">
                        <Label Content="Telefon *" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,10">
                        <Label Content="E-posta" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
                        <Label Content="Şehir *" Style="{StaticResource ModernLabel}"/>
                        <ComboBox ItemsSource="{Binding Cities}" SelectedItem="{Binding SelectedCity}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,10,10">
                        <Label Content="İlçe" Style="{StaticResource ModernLabel}"/>
                        <ComboBox ItemsSource="{Binding Districts}" SelectedItem="{Binding SelectedDistrict}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,10">
                        <Label Content="Mahalle" Style="{StaticResource ModernLabel}"/>
                        <ComboBox ItemsSource="{Binding Neighborhoods}" SelectedItem="{Binding SelectedNeighborhood}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,0">
                        <Label Content="Sokak/Cadde" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,10,0">
                        <Label Content="Bina No" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding BuildingNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,0">
                        <Label Content="Daire No" Style="{StaticResource ModernLabel}"/>
                        <TextBox Text="{Binding ApartmentNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="Kaydet" Command="{Binding SaveCustomerCommand}" Style="{StaticResource ModernButton}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="Temizle" Command="{Binding ClearFormCommand}" Style="{StaticResource SecondaryButton}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="Sil" Command="{Binding DeleteCustomerCommand}" Style="{StaticResource DangerButton}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.4*" MinWidth="280" MaxWidth="400"/>
                <ColumnDefinition Width="0.6*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardPanel}" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Konum" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="{StaticResource TextPrimaryColor}"/>

                    <Border Grid.Row="1" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                        <wv2:WebView2 Source="{Binding MapUrl, Mode=OneWay, TargetNullValue='about:blank', FallbackValue='about:blank'}"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Müşteri Listesi" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="{StaticResource TextPrimaryColor}"/>

                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔍" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource ModernTextBox}" 
                                 Tag="İsim, telefon veya şehir ara..."
                                 VerticalAlignment="Center"/>
                    </Grid>

                    <DataGrid Grid.Row="2"
                              AutoGenerateColumns="False"
                              ItemsSource="{Binding CustomersView}"
                              SelectedItem="{Binding SelectedCustomer}"
                              Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              CanUserAddRows="False">
                        <DataGrid.InputBindings>
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding EditCustomerCommand}"/>
                            <KeyBinding Key="Delete" Command="{Binding DeleteCustomerCommand}"/>
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Ad Soyad" Binding="{Binding FullName}" Width="1.5*"/>
                            <DataGridTextColumn Header="Telefon" Binding="{Binding PhoneNumber}" Width="*"/>
                            <DataGridTextColumn Header="E-posta" Binding="{Binding Email}" Width="1.5*"/>
                            <DataGridTextColumn Header="Şehir" Binding="{Binding City}" Width="0.8*"/>
                            <DataGridTextColumn Header="Adres" Binding="{Binding FullAddress}" Width="2*"/>
                            <DataGridTemplateColumn Header="İşlemler" Width="Auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Profil Görüntüle" 
                                                Command="{Binding DataContext.ViewProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                Style="{StaticResource ModernButton}"
                                                Padding="10,5"
                                                Margin="5,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>