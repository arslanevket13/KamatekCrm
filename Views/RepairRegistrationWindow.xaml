<Window x:Class="KamatekCrm.Views.RepairRegistrationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:KamatekCrm.ViewModels"
        mc:Ignorable="d"
        Title="Cihaz Kabul Ekranı" Height="650" Width="500"
        WindowStartupLocation="CenterScreen">
    
    <Window.DataContext>
        <vm:RepairViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Yeni Cihaz Kabulü" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Müşteri Seçimi -->
                <GroupBox Header="Müşteri Bilgileri" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Müşteri Seçin:" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Customers}" 
                                  SelectedItem="{Binding SelectedCustomerForNewJob}"
                                  DisplayMemberPath="FullName"
                                  IsEditable="True"
                                  Height="30"/>
                    </StackPanel>
                </GroupBox>

                <!-- Cihaz Bilgileri -->
                <GroupBox Header="Cihaz Bilgileri" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Marka:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.DeviceBrand}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Text="Model:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.DeviceModel}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Text="Seri No:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.SerialNumber}" Height="30" Margin="0,0,0,10"/>

                        <TextBlock Text="Fiziksel Durum (Çizik, Kırık vb.):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.PhysicalCondition}" Height="30" Margin="0,0,0,10"/>
                    </StackPanel>
                </GroupBox>

                <!-- Teslim Alınanlar -->
                <GroupBox Header="Aksesuarlar &amp; Durum" Padding="10" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Aksesuarlar (Çanta, Şarj Aleti vb.):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.Accessories}" Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>

                        <TextBlock Text="Arıza Açıklaması / Müşteri Beyanı:" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewJob.Description}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="İptal" Width="100" Height="35" Margin="0,0,10,0" IsCancel="True"/>
            <Button Content="Kaydı Oluştur" Width="150" Height="35" 
                    Command="{Binding SaveNewRepairCommand}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
        </StackPanel>
    </Grid>
</Window>
