<UserControl x:Class="KamatekCrm.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d"
             Background="{DynamicResource ThemeBackground}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ==================== ROW 0: HEADER ==================== -->
            <Border Grid.Row="0" Margin="0,0,0,24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding WelcomeMessage}" 
                                   FontSize="32" FontWeight="Bold" 
                                   Foreground="{DynamicResource ThemeTextPrimary}"/>
                        <TextBlock FontSize="14" Margin="0,6,0,0"
                                   Foreground="{DynamicResource ThemeTextSecondary}">
                            <Run Text="🎯 Komut Merkezi • "/>
                            <Run Text="{Binding TodayDate, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="🔄 Yenile" 
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding RefreshDashboardCommand}"
                                Height="40" Padding="20,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ==================== ROW 1: QUICK STATS ==================== -->
            <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,24">
                
                <!-- Bugünün İşleri -->
                <Border Style="{StaticResource HoverCardPanel}" Margin="0,0,12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Bugünün İşleri" FontSize="12" FontWeight="Medium"
                                       Foreground="{DynamicResource ThemeTextSecondary}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding TodaysJobsCount}" 
                                       FontSize="42" FontWeight="Bold" 
                                       Foreground="{DynamicResource ThemePrimary}" 
                                       Margin="0,4,0,0"/>
                            <TextBlock Text="planlanmış" FontSize="11" 
                                       Foreground="{DynamicResource ThemeTextTertiary}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{DynamicResource ThemePrimaryLight}" 
                                CornerRadius="16" Width="56" Height="56"
                                VerticalAlignment="Center" Opacity="0.2">
                            <TextBlock Text="📆" FontSize="24" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Teslime Hazır -->
                <Border Style="{StaticResource HoverCardPanel}" Margin="6,0,6,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Teslime Hazır" FontSize="12" FontWeight="Medium"
                                       Foreground="{DynamicResource ThemeTextSecondary}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding ReadyRepairsCount}" 
                                       FontSize="42" FontWeight="Bold" 
                                       Foreground="{DynamicResource ThemeSuccess}" 
                                       Margin="0,4,0,0"/>
                            <TextBlock Text="cihaz bekliyor" FontSize="11" 
                                       Foreground="{DynamicResource ThemeTextTertiary}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{DynamicResource ThemeSuccess}" 
                                CornerRadius="16" Width="56" Height="56"
                                VerticalAlignment="Center" Opacity="0.15">
                            <TextBlock Text="✅" FontSize="24" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Stok Uyarısı -->
                <Border Style="{StaticResource HoverCardPanel}" Margin="6,0,6,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Stok Uyarısı" FontSize="12" FontWeight="Medium"
                                       Foreground="{DynamicResource ThemeTextSecondary}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding LowStockCount}" 
                                       FontSize="42" FontWeight="Bold" 
                                       Foreground="{DynamicResource ThemeError}" 
                                       Margin="0,4,0,0"/>
                            <TextBlock Text="düşük stok" FontSize="11" 
                                       Foreground="{DynamicResource ThemeTextTertiary}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{DynamicResource ThemeError}" 
                                CornerRadius="16" Width="56" Height="56"
                                VerticalAlignment="Center" Opacity="0.15">
                            <TextBlock Text="🚨" FontSize="24" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Aktif İşler -->
                <Border Style="{StaticResource HoverCardPanel}" Margin="12,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Aktif İşler" FontSize="12" FontWeight="Medium"
                                       Foreground="{DynamicResource ThemeTextSecondary}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding ActiveJobsCount}" 
                                       FontSize="42" FontWeight="Bold" 
                                       Foreground="{DynamicResource ThemeWarning}" 
                                       Margin="0,4,0,0"/>
                            <TextBlock Text="devam ediyor" FontSize="11" 
                                       Foreground="{DynamicResource ThemeTextTertiary}"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <Border Grid.Column="1" Background="{DynamicResource ThemeWarning}" 
                                CornerRadius="16" Width="56" Height="56"
                                VerticalAlignment="Center" Opacity="0.15">
                            <TextBlock Text="🔧" FontSize="24" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- ==================== ROW 2: CHARTS ==================== -->
            <Grid Grid.Row="2" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- 7 Günlük Trend Grafiği -->
                <Border Grid.Column="0" Style="{StaticResource HoverCardPanel}" Margin="0,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,16">
                            <TextBlock Text="📈 Haftalık Performans" FontSize="16" FontWeight="SemiBold"
                                       Foreground="{DynamicResource ThemeTextPrimary}"/>
                            <TextBlock Text="Son 7 günlük gelir ve tamamlanan iş trendi" FontSize="12" 
                                       Foreground="{DynamicResource ThemeTextSecondary}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <lvc:CartesianChart Grid.Row="1" 
                                            Series="{Binding WeeklyTrendSeries}"
                                            XAxes="{Binding WeeklyTrendXAxes}"
                                            YAxes="{Binding WeeklyTrendYAxes}"
                                            Height="220"
                                            TooltipPosition="Top"
                                            ZoomMode="X"/>
                    </Grid>
                </Border>

                <!-- İş Kategorileri Dağılımı -->
                <Border Grid.Column="1" Style="{StaticResource HoverCardPanel}" Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="🎯 Aktif İş Dağılımı" FontSize="16" FontWeight="SemiBold"
                                       Foreground="{DynamicResource ThemeTextPrimary}"/>
                            <TextBlock Text="Kategori bazlı" FontSize="12" 
                                       Foreground="{DynamicResource ThemeTextSecondary}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <lvc:PieChart Grid.Row="1" 
                                      Series="{Binding JobCategoryPieSeries}"
                                      Height="220"
                                      LegendPosition="Right"
                                      InitialRotation="-90"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- ==================== ROW 3: MAIN WIDGETS ==================== -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.3*"/>
                    <ColumnDefinition Width="0.7*"/>
                </Grid.ColumnDefinitions>

                <!-- ========== WIDGET 1: KRİTİK UYARILAR ========== -->
                <Border Grid.Column="0" Style="{StaticResource HoverCardPanel}" Margin="0,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="#FEF2F2" CornerRadius="10" Padding="14" Margin="-16,-16,-16,16">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🚨" FontSize="22" VerticalAlignment="Center"/>
                                <StackPanel Margin="12,0,0,0">
                                    <TextBlock Text="Kritik Uyarılar" FontSize="15" FontWeight="Bold" 
                                               Foreground="#DC2626"/>
                                    <TextBlock Text="Stok seviyeleri" FontSize="11" 
                                               Foreground="#F87171"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Content -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="350">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding LowStockProducts}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="12" Margin="0,0,0,8" 
                                                    Background="{DynamicResource ThemeBackground}"
                                                    CornerRadius="8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding ProductName}" 
                                                                   FontWeight="Medium" FontSize="13"
                                                                   Foreground="{DynamicResource ThemeTextPrimary}"
                                                                   TextTrimming="CharacterEllipsis"/>
                                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                            <Border CornerRadius="4" Padding="8,3">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="#FFFBEB"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding UrgencyLevel}" Value="Kritik">
                                                                                <Setter Property="Background" Value="#FEF2F2"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <TextBlock Text="{Binding UrgencyLevel}" FontSize="10" FontWeight="SemiBold">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Foreground" Value="#F59E0B"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding UrgencyLevel}" Value="Kritik">
                                                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                            </Border>
                                                        </StackPanel>
                                                    </StackPanel>
                                                    <Border Grid.Column="1" CornerRadius="8" Padding="14,8"
                                                            VerticalAlignment="Center">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#EF4444"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding CurrentStock}" Value="0">
                                                                        <Setter Property="Background" Value="#991B1B"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding CurrentStock}" 
                                                                   FontWeight="Bold" FontSize="18"
                                                                   Foreground="White" HorizontalAlignment="Center"/>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Empty State -->
                                <StackPanel HorizontalAlignment="Center" Margin="0,24">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LowStockProducts.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="✓" FontSize="32" Foreground="#10B981" HorizontalAlignment="Center"/>
                                    <TextBlock Text="Tüm stoklar yeterli" FontSize="13" 
                                               Foreground="#10B981" FontWeight="Medium" Margin="0,8,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- ========== WIDGET 2: BUGÜNÜN İŞLERİ + TESLİME HAZIR ========== -->
                <Border Grid.Column="1" Style="{StaticResource HoverCardPanel}" Margin="12,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header: Bugünün İşleri -->
                        <Border Grid.Row="0" Background="#EFF6FF" CornerRadius="10" Padding="14" Margin="-16,-16,-16,16">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔧" FontSize="22" VerticalAlignment="Center"/>
                                <StackPanel Margin="12,0,0,0">
                                    <TextBlock Text="Bugünün İşleri" FontSize="15" FontWeight="Bold" 
                                               Foreground="#1D4ED8"/>
                                    <TextBlock Text="Arıza &amp; Saha Operasyonları" FontSize="11" 
                                               Foreground="#60A5FA"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Today's Jobs List -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="150">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding TodaysJobs}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="10" Margin="0,0,0,6"
                                                    Background="{DynamicResource ThemeBackground}"
                                                    CornerRadius="8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.Column="0" Background="#3B82F6" 
                                                            CornerRadius="6" Padding="10,6" Margin="0,0,12,0"
                                                            VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding ScheduledTime}" 
                                                                   FontWeight="Bold" FontSize="12"
                                                                   Foreground="White"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding CustomerName}" 
                                                                   FontWeight="Medium" FontSize="13"
                                                                   Foreground="{DynamicResource ThemeTextPrimary}"/>
                                                        <TextBlock Text="{Binding Category}" FontSize="11" 
                                                                   Foreground="{DynamicResource ThemeTextSecondary}"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Empty State -->
                                <TextBlock Text="📅 Bugün planlanmış iş yok" 
                                           Foreground="{DynamicResource ThemeTextTertiary}"
                                           FontStyle="Italic" Margin="0,16,0,0"
                                           HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TodaysJobs.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Separator: Teslime Hazır -->
                        <Border Grid.Row="2" Background="#ECFDF5" CornerRadius="10" Padding="14" 
                                Margin="-16,12,-16,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✅" FontSize="18" VerticalAlignment="Center"/>
                                <TextBlock Text="Teslime Hazır Tamirler" FontSize="13" FontWeight="Bold" 
                                           Foreground="#059669" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Ready Repairs List -->
                        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" MaxHeight="150">
                            <StackPanel>
                                <ItemsControl ItemsSource="{Binding ReadyToDeliverRepairs}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="10" Margin="0,0,0,6"
                                                    Background="{DynamicResource ThemeBackground}"
                                                    CornerRadius="8">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Border Grid.Column="0" Background="#10B981" 
                                                            CornerRadius="6" Padding="8,4" Margin="0,0,12,0"
                                                            VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding TicketNo}" 
                                                                   FontWeight="Bold" FontSize="11"
                                                                   Foreground="White"/>
                                                    </Border>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding CustomerName}" 
                                                                   FontWeight="Medium" FontSize="13"
                                                                   Foreground="{DynamicResource ThemeTextPrimary}"/>
                                                        <TextBlock Text="{Binding DeviceInfo}" FontSize="11" 
                                                                   Foreground="{DynamicResource ThemeTextSecondary}"/>
                                                    </StackPanel>
                                                    <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                                                        <TextBlock FontSize="12" Foreground="#F59E0B" FontWeight="SemiBold">
                                                            <Run Text="{Binding DaysWaiting, Mode=OneWay}"/>
                                                            <Run Text=" gün"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- Empty State -->
                                <TextBlock Text="Teslime hazır cihaz yok" 
                                           Foreground="{DynamicResource ThemeTextTertiary}"
                                           FontStyle="Italic" Margin="0,12,0,0"
                                           HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ReadyToDeliverRepairs.Count}" Value="0">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- ========== WIDGET 3: AYLIK ÖZET ========== -->
                <Border Grid.Column="2" Style="{StaticResource HoverCardPanel}" Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="#FFFBEB" CornerRadius="10" Padding="14" Margin="-16,-16,-16,16">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💰" FontSize="22" VerticalAlignment="Center"/>
                                <StackPanel Margin="12,0,0,0">
                                    <TextBlock Text="Aylık Özet" FontSize="15" FontWeight="Bold" 
                                               Foreground="#D97706"/>
                                    <TextBlock Text="{Binding CurrentMonthName}" FontSize="11" 
                                               Foreground="#FBBF24"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Content -->
                        <StackPanel Grid.Row="1" VerticalAlignment="Top">
                            
                            <!-- Total Sales -->
                            <Border Background="{DynamicResource ThemeBackground}" CornerRadius="14" Padding="20" Margin="0,0,0,16">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="TOPLAM SATIŞ" FontSize="11" FontWeight="Bold"
                                               Foreground="{DynamicResource ThemeTextSecondary}"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock FontSize="34" FontWeight="Bold" 
                                               Foreground="#10B981" HorizontalAlignment="Center"
                                               Margin="0,8,0,0">
                                        <Run Text="₺"/>
                                        <Run Text="{Binding MonthlySalesTotal, StringFormat=N0, Mode=OneWay}"/>
                                    </TextBlock>
                                    <TextBlock FontSize="12" Foreground="{DynamicResource ThemeTextSecondary}"
                                               HorizontalAlignment="Center" Margin="0,6,0,0">
                                        <Run Text="{Binding MonthlySalesCount, Mode=OneWay}"/>
                                        <Run Text=" işlem"/>
                                    </TextBlock>
                                </StackPanel>
                            </Border>

                            <!-- Quick Stats -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Completed Jobs -->
                                <Border Grid.Column="0" Background="#ECFDF5" CornerRadius="12" Padding="14" Margin="0,0,6,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="✓ Biten" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="#059669" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding MonthlyJobsCompleted}" 
                                                   FontSize="28" FontWeight="Bold" 
                                                   Foreground="#10B981" HorizontalAlignment="Center"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Active Jobs -->
                                <Border Grid.Column="1" Background="#FFFBEB" CornerRadius="12" Padding="14" Margin="6,0,0,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="⏳ Aktif" FontSize="10" FontWeight="SemiBold"
                                                   Foreground="#D97706" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding ActiveJobsCount}" 
                                                   FontSize="28" FontWeight="Bold" 
                                                   Foreground="#F59E0B" HorizontalAlignment="Center"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Daily Summary -->
                            <Border Background="{DynamicResource ThemeBackground}" CornerRadius="12" Padding="14" Margin="0,16,0,0">
                                <StackPanel>
                                    <TextBlock Text="BUGÜN" FontSize="10" FontWeight="Bold"
                                               Foreground="{DynamicResource ThemeTextSecondary}"
                                               HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock FontSize="14" Foreground="#10B981" FontWeight="SemiBold">
                                            <Run Text="↑ ₺"/>
                                            <Run Text="{Binding DailyIncome, StringFormat=N0, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock Text=" / " FontSize="14" Foreground="{DynamicResource ThemeTextTertiary}"/>
                                        <TextBlock FontSize="14" Foreground="#EF4444" FontWeight="SemiBold">
                                            <Run Text="↓ ₺"/>
                                            <Run Text="{Binding DailyExpense, StringFormat=N0, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
