<UserControl x:Class="KamatekCrm.Views.FieldJobListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource ApplicationBackgroundBrush}"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Başlık -->
            <RowDefinition Height="Auto"/>  <!-- Kategori Toggles -->
            <RowDefinition Height="Auto"/>  <!-- Filtre Bar -->
            <RowDefinition Height="*"/>     <!-- DataGrid -->
            <RowDefinition Height="Auto"/>  <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- ===== BAŞLIK ===== -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="🚜" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="SAHA OPERASYON &amp; MONTAJ LİSTESİ" 
                           FontSize="22" FontWeight="Bold" Foreground="{DynamicResource ThemeSuccess}"/>
                <TextBlock Text="Kurulum, bakım ve keşif işleri" 
                           FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            </StackPanel>
            
            <Button Content="🔄 Yenile" 
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding RefreshCommand}"
                    Margin="30,0,0,0"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <!-- ===== KATEGORİ TOGGLE BUTONLARI ===== -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="16,12" Margin="0,0,0,12">
            <Border.Effect>
                <DropShadowEffect BlurRadius="6" Opacity="0.04" ShadowDepth="1"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Kategori Filtresi" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,8"/>
                <ItemsControl ItemsSource="{Binding CategoryFilters}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                          Content="{Binding FullDisplay}"
                                          Style="{StaticResource CategoryToggleButton}"
                                          Command="{Binding DataContext.ToggleCategoryCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                          CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- ===== FİLTRE BAR ===== -->
        <Border Grid.Row="2" Style="{StaticResource FilterBarPanel}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Başlangıç Tarihi -->
                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock Text="Başlangıç" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding StartDate}"
                                Width="130"
                                Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <!-- Bitiş Tarihi -->
                <StackPanel Grid.Column="1" Margin="0,0,20,0">
                    <TextBlock Text="Bitiş" FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,4"/>
                    <DatePicker SelectedDate="{Binding EndDate}"
                                Width="130"
                                Style="{StaticResource ModernDatePicker}"/>
                </StackPanel>

                <!-- Arama (Adres) -->
                <Border Grid.Column="2" Background="{DynamicResource SurfacePageBrush}" CornerRadius="6" Padding="10,0" 
                        VerticalAlignment="Bottom" Height="34" Margin="0,0,12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔍" FontSize="14" VerticalAlignment="Center" 
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" 
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalAlignment="Center"
                                 FontSize="13"/>
                    </Grid>
                </Border>

                <!-- Temizle -->
                <Button Grid.Column="3" Content="✕ Temizle"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding ClearFiltersCommand}"
                        VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <!-- ===== DATA GRID ===== -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Opacity="0.08" ShadowDepth="2"/>
            </Border.Effect>
            
            <DataGrid ItemsSource="{Binding FilteredFieldJobs}"
                      Style="{StaticResource ModernDataGrid}"
                      RowHeight="56"
                      BorderThickness="0"
                      IsReadOnly="True">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                        <Setter Property="Background" Value="{DynamicResource ThemeSuccess}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource SurfacePageBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ThemeSuccessLight}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.InputBindings>
                    <MouseBinding MouseAction="LeftDoubleClick" 
                                  Command="{Binding OpenFieldJobDetailCommand}"
                                  CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                </DataGrid.InputBindings>

                <DataGrid.Columns>
                    <!-- Job No (Badge) -->
                    <DataGridTemplateColumn Header="İş No" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource ThemeSuccessLight}" CornerRadius="4" Padding="8,4"
                                        HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding JobNo}" 
                                               FontWeight="Bold" FontSize="11"
                                               Foreground="{DynamicResource ThemeSuccess}"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Kategori (Icon + Text) -->
                    <DataGridTemplateColumn Header="Kategori" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                    <TextBlock Text="{Binding CategoryIcon}" FontSize="16" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding CategoryDisplay}" 
                                               Foreground="{Binding CategoryColor}"
                                               FontWeight="SemiBold"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Konum (Adres + Şehir) -->
                    <DataGridTemplateColumn Header="Konum" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center" Margin="8,0">
                                    <TextBlock Text="{Binding CustomerName}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding LocationDisplay}" 
                                               FontSize="11" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Tarih/Saat -->
                    <DataGridTemplateColumn Header="Planlanan" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ScheduleDisplay}" 
                                           VerticalAlignment="Center"
                                           Margin="8,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TextHeadingBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ScheduledDate}" Value="{x:Null}">
                                                    <Setter Property="Foreground" Value="{DynamicResource ThemeWarning}"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Teknisyen -->
                    <DataGridTemplateColumn Header="Teknisyen" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                                    <Border Background="{DynamicResource ThemePrimaryLight}" CornerRadius="12" Width="24" Height="24" Margin="0,0,6,0">
                                        <TextBlock Text="👤" FontSize="12" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock Text="{Binding AssignedTechnician}" 
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Aksiyonlar -->
                    <DataGridTemplateColumn Header="" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="📍" 
                                            Style="{StaticResource IconActionButton}"
                                            ToolTip="Haritada Aç"
                                            Command="{Binding DataContext.OpenMapCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="📷" 
                                            Style="{StaticResource IconActionButton}"
                                            ToolTip="Montaj Fotoğrafları"
                                            Command="{Binding DataContext.ShowFieldPhotosCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"/>
                                    <Button Content="✅" 
                                            ToolTip="İşi Tamamla"
                                            Command="{Binding DataContext.CompleteFieldJobCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource IconActionButton}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                        <Setter Property="Opacity" Value="0.3"/>
                                                        <Setter Property="IsEnabled" Value="False"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ===== FOOTER ===== -->
        <Border Grid.Row="4" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="6" Padding="16,10" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock VerticalAlignment="Center">
                    <Run Text="Toplam:" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="Bold" Foreground="{DynamicResource ThemeSuccess}"/>
                    <Run Text="saha işi" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>

