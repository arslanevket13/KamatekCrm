<UserControl x:Class="KamatekCrm.Views.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: List -->
        <Border Grid.Column="0" Margin="0,0,16,0" CornerRadius="8" Background="{DynamicResource CardBrush}" Effect="{DynamicResource SubtleShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Search Header -->
                <Border Grid.Row="0" Background="{DynamicResource BackgroundSecondaryBrush}" Padding="16" CornerRadius="8,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                        <Button Grid.Column="1" Command="{Binding ClearSearchCommand}" Margin="8,0,0,0">
                            <ui:SymbolIcon Symbol="Dismiss24"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Suppliers List -->
                <ListBox Grid.Row="1" ItemsSource="{Binding FilteredSuppliers}" SelectedItem="{Binding SelectedSupplier}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{DynamicResource PrimaryLightBrush}" CornerRadius="50" Width="40" Height="40" Margin="0,0,12,0">
                                    <ui:SymbolIcon Symbol="Person24" Foreground="{DynamicResource PrimaryBrush}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CompanyName}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="{Binding ContactPerson}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Add Button -->
                <Button Grid.Row="2" Command="{Binding AddSupplierCommand}" Content="Yeni Tedarikçi Ekle" Margin="16" HorizontalAlignment="Stretch"/>
            </Grid>
        </Border>

        <!-- Right Panel: Details -->
        <Border Grid.Column="1" CornerRadius="8" Background="{DynamicResource CardBrush}" Effect="{DynamicResource SubtleShadow}">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,24">
                    <Border Background="{DynamicResource PrimaryBrush}" CornerRadius="50" Width="48" Height="48" Margin="0,0,16,0">
                        <ui:SymbolIcon Symbol="Building24" Foreground="White" FontSize="24"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Tedarikçi Detayları" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        <TextBlock Text="Tedarikçi bilgilerini düzenleyin" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Form Fields -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,16">
                        <TextBlock Text="Şirket Adı" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedSupplier.CompanyName, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,16">
                        <TextBlock Text="Yetkili Kişi" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedSupplier.ContactPerson, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,16">
                        <TextBlock Text="Telefon" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedSupplier.Phone, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,16">
                        <TextBlock Text="E-posta" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedSupplier.Email, UpdateSourceTrigger=PropertyChanged}" Padding="8"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2">
                        <TextBlock Text="Adres" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedSupplier.Address, UpdateSourceTrigger=PropertyChanged}" Padding="8" TextWrapping="Wrap" AcceptsReturn="True" Height="80"/>
                    </StackPanel>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                    <Button Command="{Binding DeleteSupplierCommand}" Content="Sil" Margin="0,0,8,0"/>
                    <Button Command="{Binding SaveSupplierCommand}" Content="Kaydet"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
