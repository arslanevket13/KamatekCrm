<Window x:Class="KamatekCrm.Views.CustomerAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:KamatekCrm.Helpers"
        mc:Ignorable="d"
        Title="Yeni Müşteri Ekle" Height="700" Width="600"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource PremiumWindowStyle}">

    <Window.Resources>
        <helpers:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>

        <Style x:Key="FieldLabel" TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextHeadingBrush}"/>
            <Setter Property="Padding" Value="0,8,0,4"/>
        </Style>

        <Style x:Key="FieldBox" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Style>

        <Style x:Key="ModernCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Row 0: Header -->
            <RowDefinition Height="*"/>
            <!-- Row 1: Body -->
            <RowDefinition Height="Auto"/>
            <!-- Row 2: Footer -->
        </Grid.RowDefinitions>

        <!-- ROW 0: HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="➕" FontSize="20" Margin="0,0,8,0" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="Yeni Müşteri Ekle" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextHeadingBrush}"/>
            </StackPanel>
            <Rectangle Height="1" Fill="{DynamicResource ThemeDivider}" />
        </StackPanel>

        <!-- ROW 1: BODY -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,18,0,34" Grid.RowSpan="2">
            <StackPanel Margin="0,0,16,0">
                <!-- Müşteri Tipi Seçimi -->
                <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource BorderDefaultBrush}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Müşteri Tipi" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource ThemeTextPrimary}" Margin="0,0,0,16"/>
                        <StackPanel Orientation="Horizontal">
                            <RadioButton Content="👤 Bireysel" 
                                         IsChecked="{Binding NewCustomerType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Individual, Mode=TwoWay}"
                                         GroupName="NewCustomerType" Margin="0,0,24,0" FontSize="14" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <RadioButton Content="🏢 Kurumsal" 
                                         IsChecked="{Binding NewCustomerType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Corporate, Mode=TwoWay}"
                                         GroupName="NewCustomerType" Margin="0,0,24,0" FontSize="14" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <RadioButton Content="🚶 Perakende" 
                                         IsChecked="{Binding NewCustomerType, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=WalkIn, Mode=TwoWay}"
                                         GroupName="NewCustomerType" FontSize="14" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Bireysel Müşteri Alanları -->
                <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource BorderDefaultBrush}"
                        Visibility="{Binding NewCustomerType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Individual}"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Bireysel Müşteri Bilgileri" FontWeight="SemiBold" Foreground="{DynamicResource ThemeTextPrimary}" FontSize="16" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <Label Content="TC Kimlik No" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding NewTcKimlikNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}" MaxLength="11"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Kurumsal Müşteri Alanları -->
                <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource BorderDefaultBrush}"
                        Visibility="{Binding NewCustomerType, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Corporate}"
                        Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Kurumsal Müşteri Bilgileri" FontWeight="SemiBold" Foreground="{DynamicResource ThemeTextPrimary}" FontSize="16" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="Şirket Ünvanı" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding NewCompanyName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="Vergi Numarası" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding NewTaxNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1">
                                <Label Content="Vergi Dairesi" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding NewTaxOffice, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Temel Bilgiler -->
                <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource BorderDefaultBrush}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Temel İletişim Bilgileri" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource ThemeTextPrimary}" Margin="0,0,0,16"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="Ad Soyad *" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="Telefon *" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1">
                                <Label Content="E-posta" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="1">
                                <Label Content="Doğum Tarihi" Style="{StaticResource FieldLabel}"/>
                                <DatePicker SelectedDate="{Binding BirthDate}" Height="40" FontSize="14" Padding="8,4" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Background="Transparent"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Adres Bilgileri -->
                <Border Background="{DynamicResource SurfaceCardBrush}" CornerRadius="8" Padding="16" BorderThickness="1" BorderBrush="{DynamicResource BorderDefaultBrush}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Adres Bilgileri" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource ThemeTextPrimary}" Margin="0,0,0,16"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="Şehir" Style="{StaticResource FieldLabel}"/>
                                <ComboBox ItemsSource="{Binding Cities}" SelectedItem="{Binding SelectedCity}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,16">
                                <Label Content="İlçe" Style="{StaticResource FieldLabel}"/>
                                <ComboBox ItemsSource="{Binding Districts}" SelectedItem="{Binding SelectedDistrict}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,0,16">
                                <Label Content="Mahalle" Style="{StaticResource FieldLabel}"/>
                                <ComboBox ItemsSource="{Binding Neighborhoods}" SelectedItem="{Binding SelectedNeighborhood}" DisplayMemberPath="Name" Style="{StaticResource ModernComboBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,16">
                                <Label Content="Sokak/Cadde" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding Street, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="0" Grid.Row="2">
                                <Label Content="Bina No" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding BuildingNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2" Grid.Row="2">
                                <Label Content="Daire No" Style="{StaticResource FieldLabel}"/>
                                <TextBox Text="{Binding ApartmentNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FieldBox}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Hata mesajı -->
                <Border Background="{DynamicResource StatusErrorBg}" CornerRadius="8" Padding="16,8" Margin="0,0,0,16" Visibility="{Binding HasError, Converter={StaticResource BoolVis}}">
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource StatusErrorFg}" FontSize="14" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- ROW 2: FOOTER (Eylem Butonları) -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Temizle" Command="{Binding ClearFormCommand}" Height="40" Padding="16,0" Margin="0,0,8,0" Style="{DynamicResource DefaultButtonStyle}"/>
            <Button Content="İptal" Command="{Binding CancelCommand}" Height="40" Padding="24,0" Margin="0,0,8,0" Style="{DynamicResource DefaultButtonStyle}" Click="CloseWindow_Click"/>
            <Button Content="Kaydet" Command="{Binding SaveCommand}" IsDefault="True" Height="40" Padding="32,0" Style="{DynamicResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

