<UserControl x:Class="KamatekCrm.Views.FinancialHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="{DynamicResource SurfacePageBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- KPI Cards -->
            <RowDefinition Height="300"/>  <!-- Charts -->
            <RowDefinition Height="*"/>    <!-- Detailed Grid -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Finansal Sağlık &amp; Kârlılık 💰" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextHeadingBrush}" Margin="0,0,0,20"/>

        <!-- KPI Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
                <StackPanel>
                    <TextBlock Text="Toplam Ciro (Revenue)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat=C2}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource ThemeSuccess}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
                <StackPanel>
                    <TextBlock Text="Toplam Maliyet (Cost)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="{Binding TotalCost, StringFormat=C2}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource ThemeError}"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
                <StackPanel>
                    <TextBlock Text="Net Kâr (Profit)" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock Text="{Binding NetProfit, StringFormat=C2}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource ThemeWarning}"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Charts -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Line Chart: Income vs Expense -->
            <Border Grid.Column="0" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Aylık Gelir / Gider Analizi" FontWeight="Bold" Foreground="{DynamicResource TextHeadingBrush}" Margin="0,0,0,10"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding MonthlyFinancialSeries}" XAxes="{Binding MonthlyXAxes}"/>
                </Grid>
            </Border>

            <!-- Pie Chart: Cost Breakdown -->
            <Border Grid.Column="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Tahmini Maliyet Dağılımı" FontWeight="Bold" Foreground="{DynamicResource TextHeadingBrush}" Margin="0,0,0,10"/>
                    <lvc:PieChart Grid.Row="1" Series="{Binding CostBreakdownSeries}" LegendPosition="Bottom"/>
                </Grid>
            </Border>
        </Grid>

        <!-- DataGrid: Project Profitability -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="15" Effect="{DynamicResource DropShadow}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Proje Bazlı Kârlılık Raporu (Top 20)" FontWeight="Bold" Foreground="{DynamicResource TextHeadingBrush}" Margin="0,0,0,10"/>
                
                <DataGrid Grid.Row="1" ItemsSource="{Binding ProjectProfits}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}" BorderThickness="0">
                    <DataGrid.Resources>
                         <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,5"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                         </Style>
                        <Style TargetType="DataGridCell">
                             <Setter Property="Padding" Value="10,5"/>
                             <Setter Property="Template">
                                 <Setter.Value>
                                     <ControlTemplate TargetType="DataGridCell">
                                         <Border BorderThickness="0" Background="Transparent">
                                             <ContentPresenter VerticalAlignment="Center" />
                                         </Border>
                                     </ControlTemplate>
                                 </Setter.Value>
                             </Setter>
                         </Style>
                    </DataGrid.Resources>
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Proje Adı" Binding="{Binding ProjectName}" Width="*"/>
                        <DataGridTextColumn Header="Müşteri" Binding="{Binding CustomerName}" Width="*"/>
                        <DataGridTextColumn Header="Ciro" Binding="{Binding Revenue, StringFormat=C2}" Width="100"/>
                        <DataGridTextColumn Header="Maliyet" Binding="{Binding Cost, StringFormat=C2}" Width="100"/>
                        <DataGridTextColumn Header="Kâr" Binding="{Binding Profit, StringFormat=C2}" Width="100" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Marj %" Binding="{Binding MarginPercent, StringFormat=N1}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

