<UserControl x:Class="KamatekCrm.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KamatekCrm.Views"
             xmlns:viewmodels="clr-namespace:KamatekCrm.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:KamatekCrm.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ProductViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource SurfacePageBrush}">

    <UserControl.Resources>
        <!-- Boolean to Visibility Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:ImagePathConverter x:Key="ImagePathConverter"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ════════ HEADER & MAIN ACTION ════════ -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="Stok ve Envanter Yönetimi" 
                           FontSize="28" 
                           FontWeight="Bold"
                           Foreground="{DynamicResource ThemeTextPrimary}"/>
                <TextBlock Text="Ürünlerinizi yönetin, stok takibi yapın" 
                           FontSize="14" 
                           Foreground="{DynamicResource ThemeTextSecondary}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ui:Button Content="Excel'den Yükle" 
                           Icon="{ui:SymbolIcon DocumentTable24}"
                           Command="{Binding ImportExcelCommand}"
                           Appearance="Secondary"
                           Height="40"
                           Padding="16,0"
                           Margin="0,0,16,0"/>
                
                <ui:Button Content="Yeni Ürün Ekle" 
                           Icon="{ui:SymbolIcon Add24}"
                           Command="{Binding AddNewProductCommand}"
                           Appearance="Primary"
                           Height="40"
                           Padding="16,0"
                           Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- ════════ KAYITLI ÜRÜNLER - TAM GENİŞLİK ════════ -->
        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="0">
            <Grid Margin="24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Table Header & Actions -->
                <DockPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Kayıtlı Ürünler" FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource ThemeTextPrimary}" VerticalAlignment="Center"/>
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ui:Button Content="Fotoğraf Yükle" 
                                   Icon="{ui:SymbolIcon Image24}"
                                   Command="{Binding UploadProductPhotoCommand}"
                                   Appearance="Secondary"
                                   Height="32" Margin="0,0,8,0"/>
                        
                        <ui:Button Content="Fotoğrafı Sil" 
                                   Icon="{ui:SymbolIcon Delete24}"
                                   Command="{Binding DeleteProductPhotoCommand}"
                                   Appearance="Secondary"
                                   Height="32" Margin="0,0,8,0"/>

                        <ui:Button Content="Transfer Yap" 
                                   Icon="{ui:SymbolIcon ArrowSwap24}"
                                   Command="{Binding TransferStockCommand}"
                                   Appearance="Secondary"
                                   Height="32"/>
                    </StackPanel>
                </DockPanel>

                <!-- Arama çubuğu - panel içinde -->
                <ui:TextBox Grid.Row="1"
                            PlaceholderText="Ürün Adı, SKU veya Kategori..." 
                            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                            Icon="{ui:SymbolIcon Search24}" 
                            Height="38" 
                            Margin="0,0,0,12"
                            VerticalAlignment="Center"/>

                <DataGrid Grid.Row="2" ItemsSource="{Binding ProductsView}"
                          SelectedItem="{Binding SelectedProduct}"
                          Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          GridLinesVisibility="Horizontal">
                    
                    <!-- Double-Click ile Düzenleme -->
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding EditProductCommand}"/>
                        <KeyBinding Key="Delete" Command="{Binding DeleteProductCommand}"/>
                    </DataGrid.InputBindings>
                    
                    <DataGrid.Columns>
                        <!-- Thumbnail -->
                        <DataGridTemplateColumn Header="" Width="56" CanUserSort="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="44" Height="44" CornerRadius="8"
                                            Background="{DynamicResource ThemeSidebarHover}" BorderBrush="{DynamicResource ThemeBorder}" BorderThickness="1" HorizontalAlignment="Center"
                                            ClipToBounds="True">
                                        <Grid>
                                            <!-- Placeholder icon when no image -->
                                            <TextBlock Text="📦" FontSize="22" HorizontalAlignment="Center"
                                                       VerticalAlignment="Center" Opacity="0.3">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <!-- Actual image — only shown when ImagePath has value -->
                                            <Image Stretch="UniformToFill"
                                                   Source="{Binding ImagePath, Converter={StaticResource ImagePathConverter}}">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding ImagePath}" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- SKU / Barkod -->
                        <DataGridTextColumn Header="SKU" 
                                            Binding="{Binding SKU}" 
                                            Width="Auto"
                                            MinWidth="100"/>

                        <!-- Ürün Adı -->
                        <DataGridTextColumn Header="Ürün Adı" 
                                            Binding="{Binding ProductName}" 
                                            Width="*"
                                            FontWeight="SemiBold"/>

                        <!-- Kategori -->
                        <DataGridTextColumn Header="Kategori" 
                                            Binding="{Binding Category.Name}" 
                                            Width="150"/>

                        <!-- Toplam Stok (Bold) -->
                        <DataGridTextColumn Header="Stok" 
                                            Binding="{Binding TotalStockQuantity}" 
                                            Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="{DynamicResource ThemePrimary}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Birim -->
                        <DataGridTextColumn Header="Birim" 
                                            Binding="{Binding Unit}" 
                                            Width="80"/>

                        <!-- Satış Fiyatı -->
                        <DataGridTextColumn Header="Satış Fiyatı" 
                                            Binding="{Binding SalePrice, StringFormat='{}{0:C2}'}" 
                                            Width="100"/>

                        <!-- Düzenle Butonu -->
                        <DataGridTemplateColumn Header="İşlem" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Düzenle" 
                                            Command="{Binding DataContext.EditProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource ModernButton}"
                                            Padding="16,4"
                                            Height="28"
                                            Cursor="Hand"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                    
                <!-- Status Message -->
                <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Margin="0,16,0,0" FontWeight="Medium">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource StatusErrorFg}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsActionSuccessful}" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource StatusSuccessFg}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
