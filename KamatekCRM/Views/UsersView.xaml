<UserControl x:Class="KamatekCrm.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SurfacePageBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="👥 Kullanıcı Yönetimi" 
                           FontSize="28" FontWeight="Bold"
                           Foreground="{DynamicResource TextHeadingBrush}"/>
                <TextBlock Text="Sistem kullanıcılarını görüntüle ve yönet"
                           FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Current User Info -->
            <Border Grid.Column="2" Background="{DynamicResource ThemePrimaryLight}" CornerRadius="8" Padding="16,8" Opacity="0.2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="👤" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentUserName}" FontWeight="SemiBold" FontSize="13" Foreground="{DynamicResource TextFillColorPrimaryBrush}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding CurrentUserRole}" FontSize="11" Foreground="{DynamicResource ThemePrimary}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource CardPanel}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MaxWidth="300"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search -->
                <Grid Grid.Column="0">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}"
                             Height="40" Padding="36,8,12,8"/>
                    <TextBlock Text="🔍" FontSize="16" VerticalAlignment="Center" 
                               Margin="12,0,0,0" IsHitTestVisible="False"/>
                </Grid>

                <!-- Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="🔄 Yenile"
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding RefreshCommand}"
                            Height="40" Padding="16,0" Margin="0,0,8,0"/>
                    
                    <Button Content="➕ Yeni Kullanıcı"
                            Style="{StaticResource ModernButton}"
                            Command="{Binding AddUserCommand}"
                            Height="40" Padding="16,0"
                            IsEnabled="{Binding IsAdmin}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Users Grid -->
        <Border Grid.Row="2" Style="{StaticResource CardPanel}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding Users}"
                          SelectedItem="{Binding SelectedUser}"
                          Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          RowHeight="50">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="12,0"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Ad Soyad" Binding="{Binding AdSoyad}" Width="*" MinWidth="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="12,0"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Kullanıcı Adı" Binding="{Binding Username}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="12,0"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Rol" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" Margin="12,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{DynamicResource BorderDefaultBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                        <Setter Property="Background" Value="{DynamicResource ThemeWarningLight}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Role}" Value="Technician">
                                                        <Setter Property="Background" Value="{DynamicResource ThemePrimaryLight}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="12" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                            <Setter Property="Text" Value="👑 Patron"/>
                                                            <Setter Property="Foreground" Value="{DynamicResource ThemeWarning}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Role}" Value="Technician">
                                                            <Setter Property="Text" Value="🔧 Personel"/>
                                                            <Setter Property="Foreground" Value="{DynamicResource ThemePrimary}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Role}" Value="Viewer">
                                                            <Setter Property="Text" Value="👁️ İzleyici"/>
                                                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Durum" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock VerticalAlignment="Center" Margin="12,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Text" Value="✅ Aktif"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource ThemeSuccess}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Text" Value="❌ Pasif"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource ThemeError}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Son Giriş" Binding="{Binding LastLoginDate, StringFormat={}{0:dd.MM.yyyy HH:mm}, TargetNullValue='-'}" Width="140">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Margin" Value="12,0"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Action Buttons -->
                <Border Grid.Row="1" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderDefaultBrush}" Padding="16">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <!-- Edit Button -->
                        <Button Content="✏️ Düzenle"
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding EditUserCommand}"
                                Height="36" Padding="16,0" Margin="0,0,8,0"/>

                        <!-- Set Password Button -->
                        <Button Content="🔐 Şifre Belirle"
                                Height="36" Padding="16,0" Margin="0,0,8,0"
                                Command="{Binding SetPasswordCommand}"
                                Background="{DynamicResource ThemeWarningLight}" Foreground="{DynamicResource ThemeWarning}"
                                BorderBrush="{DynamicResource ThemeWarningLight}" BorderThickness="1"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Quick Reset Button -->
                        <Button Content="🔑 1234'e Sıfırla"
                                Style="{StaticResource SecondaryButton}"
                                Command="{Binding ResetPasswordCommand}"
                                Height="36" Padding="16,0" Margin="0,0,8,0"/>
                        
                        <!-- Delete Button -->
                        <Button Content="🗑️ Sil"
                                Height="36" Padding="16,0"
                                Command="{Binding DeleteUserCommand}"
                                Background="{DynamicResource ThemeErrorLight}" Foreground="{DynamicResource ThemeError}"
                                BorderBrush="{DynamicResource ThemeErrorLight}" BorderThickness="1"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

